music extension
	TO DO
		move played item to first position in queue and the next track to start playing
		load player in background page, so player does not stop if 
		always reload iframe when navigating to new pages when the app is active
		enable/disable iframe via extension browser action
			alternate icon between bw [off] and color [on]
		enable/disable iframe via close button in iframe
		pop-out iframe via pop-out buttom
		minimize to just player via minimize button

	DONE - modal window in browser?